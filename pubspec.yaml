name: sitama
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Dependensi yang dibutuhkan oleh project
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1      # <-- Ditambahkan untuk custom font
  intl: ^0.19.0             # <-- Ditambahkan untuk format tanggal
  table_calendar: ^3.1.1    # <-- Ditambahkan untuk widget kalender

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true

  # Aset untuk gambar (indentasi sudah diperbaiki)
  assets:
    - assets/kalender.png